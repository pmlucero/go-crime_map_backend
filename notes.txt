Como ejecutar un test?
Ej. TestGetCrimeStats

go test -v go-crime_map_backend/internal/interface/controllers/tests -run TestGetCrimeStats

Detalle:
-v: muestra el output detallado (verbose)
-run TestGetCrimeStats: ejecuta solo los tests que coincidan con este patrón
go-crime_map_backend/internal/interface/controllers/tests: es la ruta al paquete que contiene el test

También se podria ejecutar desde el directorio

cd internal/interface/controllers/tests
go test -v -run TestGetCrimeStats

----------------------------
Depurador para GO que me permite ejecutar en modo debug

go install github.com/go-delve/delve/cmd/dlv@latest

Para ejecutar el testcases error_al_obtener_estadísticas en modo debug:

dlv test go-crime_map_backend/internal/interface/controllers/tests -- -test.run ^TestGetCrimeStats$/error_al_obtener_estadísticas

Usa los comandos de Delve para depurar:
b <línea>: establece un punto de interrupción en una línea específica.
c: continúa la ejecución hasta el siguiente punto de interrupción.
n: ejecuta la siguiente línea de código.
s: entra en la función llamada en la línea actual.
p <variable>: imprime el valor de una variable.

Ej:
   dlv test go-crime_map_backend/internal/interface/controllers/tests 
   -- -test.run ^TestGetCrimeStats$/error_al_obtener_estadísticas

----------------------------

Desde consola, cuando ejecuto el test en forma individual funciona correctamente:

go test ./internal/interface/controllers/tests -v -run "^TestGetCrimeStats$/error_al_obtener_estadísticas"


----------------------------

Como actualizar el swagger?
1. Instalar swag si no está instalado:
   ```bash
   go install github.com/swaggo/swag/cmd/swag@latest
   ```

2. Asegurarse de que swag está en el PATH:
   ```bash
   export PATH=$PATH:$(go env GOPATH)/bin
   ```

3. Desde la raíz del proyecto, ejecutar:
   ```bash
   swag init -g cmd/api/main.go
   ```

----------------------------
Server: Run en modo desarrollo

# Instrucciones para ejecutar el servidor en modo desarrollo

1. Asegúrate de que la base de datos esté corriendo y sea accesible con las credenciales del archivo .env:
   ```
   DB_HOST=localhost
   DB_PORT=5432
   DB_USER=postgres
   DB_PASSWORD=postgres
   DB_NAME=crime_map
   ```

2. Ejecutar el servidor directamente (desde la raíz del proyecto):
   ```bash
   go run cmd/api/main.go
   ```

3. El servidor estará disponible en:
   - API: http://localhost:8080
   - Documentación Swagger: http://localhost:8080/swagger/index.html
   - Endpoint de salud: http://localhost:8080/health

4. Para probar que el servidor está funcionando:
   ```bash
   curl http://localhost:8080/health
   ```

Notas:
- El servidor se ejecuta en modo debug por defecto
- Los logs se muestran en la consola
- Hot-reload: Para desarrollo, puedes usar air para recargar automáticamente:
  ```bash
  # Instalar air
  go install github.com/cosmtrek/air@latest

  # Ejecutar con air
  air
  ```

  ==========================
  Ejecución del test de cobertura

go test -coverpkg=./... ./... -coverprofile=coverage.out

echo "=== Database ===" && go test -coverpkg=go-crime_map_backend/internal/infrastructure/database -cover ./internal/infrastructure/database/... -coverprofile=database_coverage.out && go tool cover -func=database_coverage.out && echo "\n=== Controllers ===" && go test -coverpkg=go-crime_map_backend/internal/interface/controllers -cover ./internal/interface/controllers/... -coverprofile=controllers_coverage.out && go tool cover -func=controllers_coverage.out && echo "\n=== Mocks ===" && go test -coverpkg=go-crime_map_backend/internal/mocks -cover ./internal/mocks/... -coverprofile=mocks_coverage.out && go tool cover -func=mocks_coverage.out && echo "\n=== Usecases ===" && go test -coverpkg=go-crime_map_backend/internal/usecases -cover ./internal/usecases/... -coverprofile=usecases_coverage.out && go tool cover -func=usecases_coverage.out && echo "\n=== Utils ===" && go test -coverpkg=go-crime_map_backend/internal/utils -cover ./internal/utils/... -coverprofile=utils_coverage.out && go tool cover -func=utils_coverage.out

go test -coverpkg=./... ./internal/usecases/tests -coverprofile=coverage.out && go tool cover -func=coverage.out